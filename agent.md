# Agent Instructions - DXF Nesting System

## Role
你是一名资深 CAD 算法工程师，负责设计并实现一个 DXF 文件自动排板系统。系统需要读取 DXF 文件中的几何图形，识别可排板的规格板（大封闭图形，含挖孔），在指定尺寸的大板内进行二维不规则排样，并将最终结果写回 DXF 文件。项目目标是最大化大板出材率。

## Project Overview
- 输入：一个或多个规格板 DXF 文件 + 大板尺寸参数（长、宽、厚度）
- 输出：带排板结果的 DXF 文件 + 出材率报告
- 运行环境：Linux / Windows
- 语言：Python 3.10+
- 核心依赖：ezdxf、shapely、numpy

## Key Concepts

### 1. 大板（板材）
- 大板为矩形
- 用户指定大板的长（X 方向）、宽（Y 方向）、厚度
- 排板区域 = 大板全部区域（无边距，规格板边缘可与大板边缘重合）
- 规格板禁止超出大板边缘

### 2. 规格板（大封闭图形）
- 由 DXF 中的 LWPOLYLINE、POLYLINE、CIRCLE、ARC、SPLINE 构成
- 必须是闭合（closed）实体
- 一个规格板可能包含多个"挖孔"（小封闭图形）

### 3. 挖孔（小封闭图形）
- 完全位于某个规格板内部
- 表示从规格板中挖去的区域
- 挖孔必须随规格板一起移动，保持相对位置不变

### 4. 单位制
- 支持英制（inch）和公制（mm）
- 根据排板任务所在地区选择单位制
- 所有尺寸计算和输出统一使用所选单位

## Workflow（不可跳过）

1. 用户选择单位制（英制 / 公制）
2. 用户输入大板尺寸：长、宽、厚度
3. 系统请求 stonebot_artificial_layout 提供规格板 DXF 文件
4. 解析 DXF 文件，提取所有规格板及其挖孔
5. 为每个规格板编号：
   - 若规格板已有编号（TEXT / MTEXT 标注），识别并保留该编号
   - 若规格板无编号，自动生成编号，格式：P-0001、P-0002 ...
   - 编号位置：位于规格板的几何中心
   - 编号要求：使用 TEXT 实体，字高 3~5mm，清晰可读
6. 按面积从大到小对所有规格板排序
7. 排板：
   a. 从面积最大的规格板开始，放入第一张大板
   b. 规格板可旋转 0°、90°、180°、270° 以求更优摆放
   c. 第一块规格板摆放完成后，在大板剩余空间中寻找能放下的最大面积规格板
   d. 重复步骤 c，直至当前大板无法再放入任何规格板
   e. 开启下一张大板，重复步骤 a~d，直至所有规格板排完
8. 输出排板结果 DXF 文件
9. 输出出材率报告

## Nesting Rules

### 排板规则
1. 规格板必须完整放入大板区域内
2. 规格板边缘可与大板边缘重合，但禁止超出大板边界
3. 规格板之间禁止相交（几何重叠），但允许边缘重合（共边）
4. 挖孔随所属规格板整体移动，不能单独处理
5. 规格板可按 90° 的倍数旋转（0°、90°、180°、270°）
6. 排板策略：贪心最大优先（Greedy Best-Fit Decreasing）
   - 始终优先放置面积最大的规格板
   - 当前大板排满后自动启用下一张大板
7. 目标：最小化大板使用数量，最大化出材率

### 编号规则
- 识别并保留 DXF 中已有的规格板编号
- 无编号的规格板自动生成 P-0001、P-0002 ...
- 编号随规格板一起移动、旋转
- 编号禁止在排板结果中丢失

## DXF Output Requirements

### 文件输出
- 原文件：xxx.dxf
- 排板结果：xxx_nested.dxf
- 输出文件只包含排板结果，不复制原始图形

### Layer 结构（推荐）
- NESTED_RESULT：排板结果
  - 每张大板一个红色外框 + Sheet 标签
  - 每块规格板的外轮廓（含挖孔）
  - 每个规格板附带居中编号 TEXT

### 排板结果必须包含
1. 大板外框（矩形）
2. 所有规格板（含挖孔）
3. 每个规格板的编号 TEXT
4. 大板编号（Sheet_1、Sheet_2 ...）

## Output Data（控制台 / JSON）
程序运行结束后必须输出以下内容：

大板尺寸：2440 x 1220 x 18 mm
单位制：公制
使用大板数量：3 张
每张大板排板明细：
  Sheet 1: P-0001, P-0002, P-0005
  Sheet 2: P-0003, P-0006, P-0007
  Sheet 3: P-0004, P-0008

所有规格板总面积：28400 mm²
大板总面积：73200 mm²
出材率：38.8%

## Algorithm Requirements

### 必须使用
- Shapely：多边形构建、布尔差集、碰撞检测
- ezdxf：DXF 读写
- 启发式算法：贪心（Best-Fit Decreasing）+ 遗传算法 / 模拟退火（可选优化）

### 排板流程（不可跳过）
1. 解析 DXF，提取所有封闭轮廓
2. 判断内外包含关系，构建带洞多边形
3. 计算每块规格板的面积
4. 按面积降序排列
5. 初始化第一张大板（无边距）
6. 依次尝试放置每个规格板：
   - 尝试不同位置
   - 尝试不同旋转角度（0°/90°/180°/270°）
   - 优先放置面积最大的规格板
   - 大板剩余空间内寻找能放下的最大规格板
   - 若当前大板无法放入任何规格板，则新建大板
7. 排板完成后统计结果并写出 DXF

## Code Style Rules
- 所有长度单位统一（根据所选单位制）
- 使用 Python dataclass 定义 Part、Sheet
- 每个模块职责单一
- 关键算法必须添加中文注释
- 几何变换统一使用 shapely.translate / rotate

## Constraints（红线）
- 禁止修改原始图形的几何形状
- 禁止忽略挖孔
- 禁止规格板超出大板边界
- 禁止在结果 DXF 中丢失编号
- 禁止规格板相交重叠（边缘重合允许）
- 禁止删除原始 DXF 中的任何实体

## Example Task Prompt（给 Codex 用）

请参考 agent.md，实现 DXF 排板系统的核心模块：
1. 支持英制/公制切换
2. 接收大板尺寸输入（长、宽、厚度）
3. 请求并读取 stonebot_artificial_layout 提供的规格板 DXF
4. 识别所有规格板及挖孔，完成编号
5. 按贪心最大优先策略排板
6. 将排板结果写回新的 DXF 文件
7. 输出出材率和大板使用情况

## Notes
- 本系统用于实际生产环境
- 排板结果 DXF 将直接用于 CNC / 激光 / 切割机
- 精度与稳定性优先于速度
