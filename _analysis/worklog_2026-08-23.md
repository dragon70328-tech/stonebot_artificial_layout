# 工作日志 - 2026-08-23

## 概述

启动加拿大3L 新图纸的识图排板工作，大板尺寸固定为 `3200x1800x20mm`。完成图纸画像、读图、审图、原位编号检查，并将排板结果收敛到 **24 张板**。测试基线：`89 passed`。

## 输入文件

- `C:\Users\drago\Desktop\临时文件\加拿大3L.dxf`

## 图纸分析结论

- DXF 版本 `AC1032`，`$INSUNITS=4`。
- 图层分布：
  - `0`：面板边界、孔洞、部分区域标签，主读取层。
  - `编号`：96 个编号文本，格式如 `302-A`、`305-D`。
  - `大样`、`标注`、`说明`、`框`：不参与排板。
- 实体分布：
  - `LWPOLYLINE` 365，`CIRCLE` 72，`LINE` 128，`TEXT` 166，`MTEXT` 32，`DIMENSION` 690，`INSERT` 8。
- 面板与孔洞：
  - 面板 `96` 件。
  - 孔洞 `144` 个。
  - 编号 `96` 个，全部唯一。
  - 其中 `317-A`、`318-B` 两件由图层 `0` 的 LINE 链组成；其内部闭合 LWPOLYLINE 和圆孔需要作为孔洞处理。

## 新建图纸画像

- `drawing_profiles/canada3l.json`
- 规则摘要：
  - `panel_layers`: `["0"]`
  - `hole_layers`: `["0"]`
  - `number_layers`: `["编号"]`
  - `label_pattern`: `^(?P<zone>\\d+)-(?P<part>[A-Z])$`
  - `build_hierarchy`: `true`
  - 排除线型：`ZIGZAG`、`DASH`、`PHANTOM`
  - 审图忽略规则：低置信度 LINE 面板、边界上的编号、辅助 LINE/LWPOLYLINE。
- 审图结果：`0` 个问题。

## 代码改动

- `src/drawing_profile.py`
  - 在构建孔洞层级时补记 `hole_handles`，避免孔洞重复写入。
  - 扩展 `_assign_profile_holes`：
    - 支持 `hole_layers` 中的闭合 `LWPOLYLINE`/`POLYLINE` 孔洞。
    - 支持 `CIRCLE` 孔洞。
    - 对已归属孔洞按 handle 去重。
    - 将实际作为孔洞、但被误提取为面板的孤立图形移除。

## 排板过程

- 快速首轮：`--quick --budget 0`，结果为 `25` 张板，出材率 `69.25%`。
- 完整桥切优化：
  - `--profile min_sheets --budget 120 --trials 2`
  - 结果：`24` 张板，出材率 `72.13%`，校验通过。
  - 输出目录：`output/20260823_101944_加拿大3L`
- 尝试压到 23 张：
  - 桥切 `3 x 60s`：仍为 `24` 张。
  - 桥切 `1 x 300s`：仍为 `24` 张。
  - 水刀 DeepNest 快速：`29` 张，更差。
  - 水刀 DeepNest + 配对：无法放置配对单元，失败。
  - 结论：当前引擎与约束下未能找到 23 张可行解。

## 最终保留结果

- 方案：桥切 `min_sheets`，`rotation=[0,90,180,270]`，`min_gap=0`，后处理推边 + 边缘对齐。
- 结果：`24` 张板，`96` 件，出材率 `72.13%`，校验通过。
- 主要输出：
  - `output/20260823_101944_加拿大3L/加拿大3L_numbered_原位.dxf`
  - `output/20260823_101944_加拿大3L/加拿大3L_nested_3200x1800.dxf`
  - `output/20260823_101944_加拿大3L/加拿大3L_report_3200x1800.json`

## 当前未提交变更

- `drawing_profiles/canada3l.json`（新增）
- `src/drawing_profile.py`（孔洞处理增强）

## 下一步

- [ ] 在 CAD 中人工复核加拿大3L 原位编号图，重点确认 `317-A`、`318-B` 和 `305-D`。
- [ ] 确认最终保留 24 张方案后，再决定是否提交本次画像与孔洞处理改动。
