# 工作日志 - 2026-08-15

## 概述

无代码改动。通读《AI 智能排板工作流改进方案》（docs/ai-workflow-improvement-plan.md），
完成现状与计划的差距分析，并就项目结构精简做了一轮讨论（只讨论未动手）。

## 完成

### 1. 改进计划解读 + 现状差距确认

计划核心：CLI 升级为对话式工作流（读图人类语言报告 → 编号确认 → 原位 DXF 反复修改
→ 排板参数确认 → 进度可视排板 → 先报板数 → 确认出排板 DXF → 确认后再后处理出 final 版），
配套任务状态机、打断/回退、让尺 SheetAllowance 配置、约束外置、SaaS 部署。

现状核实结论：

- `src/nesting.py` 的 `nest_parts` 已有 `progress` 回调（轮次/最优板数），Phase 1 进度接入
  只需扩展到后处理和读写文件；无任何取消机制。
- 输出已按时间戳子目录隔离，天然适配"中间文件版本化/回退"。
- 让尺 auto 判断（`part_fits` 分流 + 两两配对）已在 `main.py` 跑通，Phase 4 主要是建模
  为 `SheetAllowance`，不是重写算法。
- 历史 `app/` 前端（Flask + DeepSeek，commit 8e4d29d 等）已从工作区删除，仅存 git 历史，
  Phase 2 需重建。
- 计划 Step 8/9 要求"排板 DXF 先出未后处理版、确认后再出 final 版"，与现有 CLI
  （后处理完才写一次 DXF）行为冲突，实施时需定：CLI 默认新流程还是 `--no-confirm` 保持旧行为。

### 2. 项目结构精简讨论（未动手，结论如下）

A 类（零风险清理，随时可做）：
- `template_v3.py` 实为死代码：sys.path 指向旧项目 `C:\Users\drago\stonebot_artificial_layout`
  （该目录仍存在所以"能跑"），硬编码 Desktop 路径，不可复现。删除或归档待 drago 定。
- `.env` / `.env.example` 是已删除 app/ 前端的 DeepSeek 配置，当前无归属；
  `.env` 已 gitignore 无泄密风险。
- `output/` 根目录残留一个时间戳约定前的散文件 `水星项目户型及排板_numbered_原位.dxf`。
- 根目录两个 `_backup_*.zip` 建议挪到仓库外。

B 类（结构精简，与 Phase 1 绑定做，不单独动）：
- 编号逻辑约 200 行住在 `src/dxf_reader.py`（`_collect_number_texts`、
  `_collect_room_texts`、`_assign_numbers_by_containment` 等），`src/numbering.py`
  仅 35 行壳。拆 `run()` 前先把匹配逻辑迁回 numbering.py，reader 只留几何提取。
- `main.py` 约 180 行特殊板配对几何代码（11 个函数）抽到 `src/special_nesting.py`，
  不碰 `nesting.py` 的意图仍满足；连带修改仅 `tests/test_special_pairing.py`
  的 `import main as app` 一行。

C 类（顺手）：
- `.gitignore` output 段叠床架屋：扁平时代 `output/*.dxf` + 三条 `!` 例外 + 后来的
  `output/*_*/`；其中例外里的中文是 GBK 乱码字节，靠字节级巧合工作，很脆。
  可简化成两三行。
- AGENTS.md 项目结构漂移：列了不存在的 `sample/`，numbering.py 描述与实际不符，
  随 Phase 1 收尾时同步更新。

不建议动：`units.py`、`models.py` 小而内聚；`_analysis/` 历史日志不回改，以后统一 UTF-8。

### 3. 商定方向

- Phase 1（WorkflowSession + 拆 run() + 进度/取消 + 三确认点）是下一步主线。
- B 类两项迁移作为 Phase 1 的第一步，先摆正职责再套状态机。
- A 类清理随时可做；C7 顺手；C8 随 Phase 1 收尾。

## 关键经验

- 排查死代码要验证依赖是否还存在：template_v3.py 看似可用，实则依赖旧项目目录硬编码路径。
- .gitignore 中文模式按字节匹配，GBK/UTF-8 混用时可能"靠巧合工作"，排查时用
  `git check-ignore -v` 验证实际生效的规则。

## 待续

- [ ] drago 决定 template_v3.py：删除（git 历史可恢复）还是归档到 `_archive/`。
- [ ] A 类清理：散文件归档、备份 zip 挪出仓库、.env 系列处置。
- [ ] Phase 1 启动：先迁移编号逻辑和特殊板配对（B 类），再抽 WorkflowSession 拆 run()。
- [ ] 定 CLI 行为：默认新两确认点流程 vs `--no-confirm` 兼容旧行为。
- [ ] docs/ai-workflow-improvement-plan.md、docs/constraint-externalization.md 提交入库。
