# 工作日志 2026-08-28

## 主题
深入读图模块：DXF 开放网格重建、曲线分割件识别、福州万象城 1.1 的容差补层。

## 已完成

### 1. 图纸分析方法与指纹
- 记录 Block D 6F 地台石读图方法，生成图纸指纹：
  - 画像：`drawing_profiles/blockd_6f_lobby.json`
  - 验证用例：`drawing_profiles/validated_cases/blockd_6f_lobby.json`
  - 说明文档：`docs/drawing-profile-blockd-6f-lobby.md`
  - 方法：PDF 导入的开放 LWPOLYLINE 重建为 4x12 矩形网格；2/3/34/35 四格内部闭合曲线拆分为 `2a/2b`、`3a/3b`、`34a/34b`、`35a/35b`。
  - 预期：52 个面板、0 个孔、52 个编号。
- 将同类方法复用到 `福州万象城1.dxf`：
  - 画像：`drawing_profiles/fuzhou_wanxiangcheng1.json`
  - 只取 `ST-01水刀`、`ST-02整砖`、`ST-12整砖` 内封闭图形；F 层开放网格只作区域指示，不作为规格板。
  - 预期：133 个面板、36 个孔。

### 2. 福州万象城 1.1 开放网格重建
- 新增 `network_mode` 读图能力，核心在 `src/drawing_profile.py`：
  - 将 LINE / LWPOLYLINE / ARC 网络合并后 `polygonize`，从开放线段恢复封闭图形。
  - `network_snap_tolerance=1.5`：端点吸合，解决边界未完全封闭的识别问题。
  - `network_gap_side_tolerance=3.0`：过滤 2mm 预留伸缩缝形成的窄条，不作为规格板。
  - 面积范围：`network_min_area=1000`，`network_max_area=50000000`。
- 新增画像：
  - `drawing_profiles/fuzhou_wanxiangcheng1_1.json`
  - `drawing_profiles/validated_cases/fuzhou_wanxiangcheng1_1.json`
- 规则：封闭后矩形边长差为 2mm 的是伸缩缝，不排板；其他封闭图形，包括曲线分割后的非矩形封闭图形，均作为规格板。

### 3. 修复两条线未被识别
- 原因：`fuzhou_wanxiangcheng1_1` 画像只配置了 `F-地材填充` 和 `F-地材造型线` 参与网格重建。
- 选中的两条线位于 `0`、`灯柱定位线`、`现场实际间隔柱定位`，因此被忽略。
- 处理：将这三个图层加入 `panel_layers` 与 `network_layers`。
- 结果更新：
  - 面板：6811 -> 8001
  - 孔洞：169 -> 171
  - 审图：匹配 `fuzhou_wanxiangcheng1_1`，score 100，0 个问题。

## 验证

- 画像匹配与审图：
  - `python -X utf8 main.py "C:\Users\drago\Desktop\临时文件\福州万象城1.1.dxf" --audit`
  - 结果：`审图完成：发现 0 个问题`
- 原位带编号 DXF：
  - `C:\Users\drago\stonebot_artificial_layout_architecture\output\20260828_171241_福州万象城1.1\福州万象城1.1_numbered_原位.dxf`
  - 识别 8001 个面板。
- 完整测试：
  - `python -X utf8 -m pytest -q`
  - 结果：`110 passed`

## 当前未提交改动

- 修改：
  - `main.py`
  - `src/drawing_profile.py`
  - `src/dxf_reader.py`
  - `tests/test_drawing_profile.py`
  - `tests/test_dxf_reader_numbering.py`
- 新增：
  - `docs/drawing-profile-blockd-6f-lobby.md`
  - `drawing_profiles/blockd_6f_lobby.json`
  - `drawing_profiles/fuzhou_wanxiangcheng1.json`
  - `drawing_profiles/fuzhou_wanxiangcheng1_1.json`
  - `drawing_profiles/validated_cases/blockd_6f_lobby.json`
  - `drawing_profiles/validated_cases/fuzhou_wanxiangcheng1_1.json`
  - `src/recognized_contract.py`
  - `tests/test_recognized_contract.py`

## 明天继续

- 打开新的 `福州万象城1.1_numbered_原位.dxf`，人工确认原先漏识别的两条线及其周边面板编号正确。
- 确认无误后，提交或备份今天的读图模块改动。
- 检查 Block D 与福州万象城同类图纸的画像复用是否还有可统一的字段，避免重复配置。
- 若人工审图发现新的漏识别/误识别，优先补充容差参数或图层规则，并同步更新 validated case 的指纹与 expected counts。
