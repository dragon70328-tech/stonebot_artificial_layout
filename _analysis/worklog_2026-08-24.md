# 工作日志 - 2026-08-24

## 今日工作概览

围绕加拿大3L 项目做了一次 3230x1830 圆盘锯排板测试，目标是贴近人工基准：
人工排板 22 张，自动排板最终达到 22 张；继续尝试 21 张未成功，相关试验性算法改动已回退。

## 基线状态

- 项目为 DXF -> 读图/编号 -> 审图 -> 排板 -> 输出 DXF/报告 的 CLI 工具。
- 测试基线：`python -m pytest -q`，`89 passed`。
- 当天排板输入：
  `output\20260823_103827_加拿大3L\加拿大3L_numbered_原位.dxf`
- 大板：3230 x 1830 mm，厚度 20 mm，圆盘锯直线切割，采用桥切机 `bridge`。

## 排板测试结果

| 配置 | 结果 |
|------|------|
| `--profile min_sheets --quick --budget 0` | 24 张，出材率 70.29% |
| `--budget 180 --trials 2` | 23 张，出材率 73.35% |
| `--budget 300 --trials 2 --seed 11` | **22 张，出材率 76.68%** |
| `--budget 600 --trials 2 --seed 23` | 23 张，出材率 73.35% |
| `--process waterjet --rotation "0,90,180,270" --quick` | 27 张，出材率 62.48% |

结论：桥切 `min_sheets` + 长 LNS 优于水刀 BLF；结果对随机种子敏感，`seed=11` 是复现 22 张的关键。

## 21 张排板尝试（未成功）

- 试验性增强 `src/nesting.py` 的 `_lns_improve`：
  - 扩大拆板数量范围。
  - 面积可行时始终先尝试压缩一张板。
  - 周期性全量压板搜索 `len(best)-1` 张。
- 单元测试仍 `89 passed`。
- 多次搜索停在 22/23 张，未找到 21 张可行解。
- 为避免引入未经验证的算法改动，已把 `src/nesting.py` 恢复到 HEAD 版本。

## 已确认的 22 张配方

输入文件：
`output\20260823_103827_加拿大3L\加拿大3L_numbered_原位.dxf`

命令：
```
python main.py "output\20260823_103827_加拿大3L\加拿大3L_numbered_原位.dxf" 3230 1830 20 --profile min_sheets --budget 300 --trials 2 --seed 11 --no-confirm
```

关键参数：
- 工艺：桥切机 `bridge`，`rotation=[0,90,180,270]`，`min_gap=0`
- 后处理：`slide_to_edge + align_edges`
- 搜索：`budget=300s/轮`，`trials=2`，`seed=11`
- 结果：22 张，96 件，出材率 76.68%，校验 0 错误

输出目录：
`output\20260824_193133_加拿大3L_numbered_原位\`

## 当前 Git 状态

- `src/drawing_profile.py`：昨日遗留的未提交改动（孔洞处理增强）。
- `drawing_profiles/canada3l.json`：新增，未提交。
- `_analysis/worklog_2026-08-23.md`、`_analysis/worklog_2026-08-24.md`：工作日志，未提交。
- `src/nesting.py`：已恢复到 HEAD，无差异。