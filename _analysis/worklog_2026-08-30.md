# 工作日志 2026-08-30

## 主题
水星项目识图闭环、异常闭合顶点通用恢复，以及当前整体成果与后续改进盘点。

## 已完成

### 1. 水星异常闭合顶点修复
- 输入：`C:\Users\drago\Desktop\临时文件\水星项目户型及排板.dxf`
- 画像：`drawing_profiles/mercury.json`
  - `expected_counts = {panels: 1258, holes: 0, numbers: 1258}`
- 原图 LWPOLYLINE 统计：
  - 共 `1264` 条
  - `closed=True`：`1178`
  - `closed=False` 但可恢复：`80`
  - 2 顶点残留无效线段：`6`
- 典型多余顶点模式：
  - 首点在前方重复，尾部再跟冗余点：`[0,1,2,3,0,extra]`
  - 末点等于第二个顶点：`[0,1,2,3,1]`
  - 连续重复顶点
- 通用处理已固化在 `src/dxf_reader.py`：
  - `_dedupe_consecutive_points()`
  - `_closed_loop_points()`
  - `_is_recoverable_closed()` 接受“首点重复闭合”
  - `buffer(0)` 修复无效几何，`MultiPolygon` 取最大面
- 只读验证结果：`read_dxf_with_profile()` 返回 `1258` 个面板，与画像预期一致。

### 2. 回归测试
- 新增用例：`tests/test_dxf_reader_numbering.py`
  - 首点重复 + 尾点冗余
  - 末点等于第二个顶点
  - 连续重复顶点
- 单文件测试：`15 passed`
- 全量测试：`171 passed`

### 3. 文档沉淀
- `docs/drawing-intake-subsystem.md` 新增“封闭多段线恢复（水星经验）”章节。
- 记录画像侧适配方式：`closed_tolerance` + `expected_counts`。
- 明确新项目遇到同类顶点问题时无需改 Python，先调画像验收，确有新异常模式再补回归夹具。

## 当前整体成果

- `docs/improvement-plan.md` 中 P0 到 P3 主线已完成，主要包括：
  - P0：`part_not_on_edge` 诊断口径、CGR45 通切约束分类、水刀/激光工艺检查、`--check-only` 退出码契约。
  - P1：读图/画像/编号/材料分组抽到 `src/workflow.py`，新增 `ProjectConfig`，外置读图与编号规则。
  - P2：工作流状态机 `src/workflow_session.py`、内容寻址产物 `src/artifact_store.py`、慢回归与基线测试。
  - P3：任务队列 `src/task_queue.py`、输入资源边界 `src/input_guard.py`、多租户配额 `src/tenant_store.py`、AI 规划层 `src/ai_planning.py`。
- 当前全量测试：`171 passed`。
- 多案例回归脚本：`scripts/check_output_cases.py`，覆盖加拿大 3L、CGR45、Block D、水星、东莞奥特莱斯 1/2。

## 验证

```powershell
python -X utf8 -m pytest tests/test_dxf_reader_numbering.py -q
python -X utf8 -m pytest -q
python -X utf8 -c "from src.drawing_profile import load_profile, read_dxf_with_profile; p=load_profile('drawing_profiles/mercury.json'); parts,_=read_dxf_with_profile(r'C:\Users\drago\Desktop\临时文件\水星项目户型及排板.dxf', p); print(len(parts))"
```

结果：

- `tests/test_dxf_reader_numbering.py`：`15 passed`
- 全量：`171 passed`
- 水星只读识别：`1258`

## 还需改进

1. 水星完整桥切排板回归仍需约 `9~10` 分钟，当前不适合纳入高频迭代；需要继续优化桥切贪心/LNS 的几何调用次数。
2. `main.py` 仍约 `1534` 行，读图阶段已抽到 `workflow.py`，但排板、后处理、写文件编排尚未完全外置。
3. 水星顶点恢复已覆盖当前三类模式，但未来新异常顶点仍需按文档流程补回归夹具，避免回到项目特判。
4. `scripts/check_output_cases.py` 的慢案例仍需更明确的跳过/必跑策略和 CI 超时配置。
5. SaaS 组件目前主要是“契约与内存实现”，尚未接到本地 CLI、持久化队列、真实 Worker 和外部租户存储。
6. 加拿大 3L 的 22 张最优结果仍依赖 `--budget 180`；短预算下能否稳定达到同等板数还需算法验证。

## 当前未提交改动

- 本轮新增/修改：`src/dxf_reader.py`、`tests/test_dxf_reader_numbering.py`、`docs/drawing-intake-subsystem.md`
- 路线图更新：`docs/improvement-plan.md`
- 工作日志：`_analysis/worklog_2026-08-30.md`
- 仍有多项前期未提交改动与未跟踪文件，提交前需统一 review。

## 下午追加：路线图第 1~4 步执行

### 1. 分批提交（已完成）
- 4 个提交：P0/P1 执行核心、P2 状态机与产物、P3 SaaS 边界、日志基线。

### 2. CLI 挂状态机（已完成）
- `main.run()` 各阶段边界做 `WorkflowSession` 迁移记录；`ArtifactStore.track()` 只登记 digest 不复制文件。
- 输出目录新增 `{stem}_workflow_session.json`；新增 `tests/test_cli_session_recording.py`（2 例）。

### 3. main.py 拆解（已完成）
- 排板/特殊板配对/校验/报告编排搬入 `src/workflow.py`：`nest_groups`/`postprocess_result`/`build_check_payload`/`build_report`。
- `main.py` 1534 → 1166 行；兼容导入保留（`app.nest_special_parts` 等）。

### 4. 性能攻坚（部分完成）
- 几何加速（`src/nesting.py`）：坐标数组重建代替 affinity.translate；numpy 包围盒掩码代替逐件 STRtree 重建；
  prepared intersects+touches 代替 relate_pattern；候选多边形批量构建；中心对称旋转形态去重。
- 结果：加拿大排板段 46.8s → 4.6s；水星排板段 565s → 393s（1258 件版本）。
- 未达标项：水星完整回归仍约 8.3 分钟（目标 ≤3 分钟）；加拿大 22 张仍需 `--budget 180`（201s），`--budget 60` 为 23 张。
- 过程教训：`_overlap_indices` 掩码曾把 `y0` 误写为 `y1`，正上方候选漏检导致压叠；
  由 `validate_nesting` 与回归测试当场拦截，已补 `_overlap_indices` 边界单测。

### 5. 读图修正：编号检查文件回读丢件（已完成）
- 现象：原位编号 DXF 回读只识别 950/1258 件——通用回退只认含“编”图层，不识 `NUMBERS`。
- 修复：`dxf_reader._is_number_layer()` 增加 `NUMBERS` 约定；回读恢复 1258 件。
- 副作用：Block D 编号匹配改为 NUMBERS 优先后贪心次序变化，19 张 → 20 张（几何校验仍通过）；
  已用 stash A/B 确认与该编号修正相关，与几何加速无关。

### 6. 回归案例更新
- 水星案例切换到 8-30 重新生成的 1258 件原位文件（旧文件是顶点恢复修复前的 950 件快照）。
- 1258 件结果：129 张，出材率 85.06%，理论最少 110 张，几何通过，3 项通切未对齐警告。
- 全量测试：174 passed。

### 待办（下一轮）
- 慢回归分级（fast/slow 标记 + CI 超时）与基线刷新（加拿大 25 张/21.4s、水星 950 件等基线已过期）。
- 水星后处理/校验阶段约 100s 与读图 13s 尚未优化。
- 加拿大 22 张低预算复现：LNS 破坏算子（最低利用率板/相邻零件簇）候选方向。
- blockd 19→20 属于编号匹配修正后的贪心次序变化，可后续用 LNS 预算拉回。
