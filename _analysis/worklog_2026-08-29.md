# 工作日志 2026-08-29

## 主题
桥切排板优化验证：加拿大 3L 3230x1830 复核、通切重叠修复、性能热点定位。

## 已完成

### 1. Git 备份与时间戳归档
- 当前远端：`github.com:dragon70328-tech/stonebot_artificial_layout.git`，分支 `master`。
- 已生成时间戳备份：`_backup_20260829_123449.zip`。
- 本次已推送提交：
  - `35bb0da feat: 同板数制造评分与靠边通切指标`
  - `dce9283 feat: 桥切后处理增加一刀通切对齐`
  - `eda1127 perf: 清单排板精确装箱缓存与DFS提速`
  - `936c358 perf: 排板引擎候选包围盒预筛`
  - `59c402b fix: 桥切通切后检测重叠并回退`

### 2. 加拿大 3L 3230x1830 复核
- 输入：`C:\Users\drago\Desktop\临时文件\加拿大3L.dxf`
- 大板：`3230 x 1830 x 20mm`，桥切 `min_sheets`，`rotation=[0,90,180,270]`
- kerf 在 DXF 桥切分支以 `--min-gap 5` 表达，未使用清单分支的 `--kerf`
- 命令：
  ```powershell
  python -X utf8 main.py "C:\Users\drago\Desktop\临时文件\加拿大3L.dxf" 3230 1830 20 --profile min_sheets --budget 180 --no-confirm --min-gap 5
  ```
- 结果：`22` 张，出材率 `76.68%`，`validation_errors=0`
- 输出目录：`output/20260829_164306_加拿大3L/`
- 对照 `--budget 15`：`23` 张 / `73.35%`，说明短预算 LNS 搜索不充分
- 对照 `--budget 0`：`24` 张 / `70.29%`

### 3. 3200x1800 回归
- `--budget 15 --min-gap 5`：`24` 张 / `72.13%`，校验通过
- `--budget 0 --min-gap 5`：`24` 张，校验通过
- 确认通切快照回退逻辑未影响原尺寸排板

### 4. 性能定位
- 默认 `--budget 180` 实测：`208.2s`，约 `3.4 分钟`，不是 30 分钟
- `cProfile` 文件：`_analysis/prof_3230_budget0.prof`
- 热点集中在 `src/nesting.py` 的桥切贪心：
  - `_find_placement`：约 1.3 万次放置搜索
  - `shapely.relate_pattern`：约 101 万次精确重叠判断
  - `affinity.translate`：约 99 万次几何平移
  - `STRtree.query`：约 92 万次空间查询
- `--budget 180` 中 LNS 约占 180s；后处理约 2.4s，DXF 读取/识别约 2.7s

### 5. 通切回退修复
- `src/postprocess.py` 新增：
  - `_snapshot_parts` / `_restore_parts`
  - `_has_overlap`
- `through_cut` 现在执行前快照，若通切后出现重叠则整体回退，避免通切破坏原合法布局

## 验证

- 桥切 3230x1830：`22` 张，`validation_errors=0`
- 桥切 3200x1800：`24` 张，`validation_errors=0`
- 测试：`python -X utf8 -m pytest -q` -> `111 passed`
- 静态检查：`git diff --check` 通过

## 当前未提交改动

- 工作日志：`_analysis/worklog_2026-08-29.md`
- 分析文件：`_analysis/prof_3230_budget0.prof`
- 早期基线/profile 文件仍为未跟踪，待决定是否归档或清理

## 下一步

- 优先给 `_find_placement` 做更早的旋转/包围盒候选裁剪，减少百万级 `relate_pattern`
- 用“同规格件先配对/成组 + 多起点局部搜索”替换或增强纯随机拆板 LNS，目标在 `--budget 30~60` 内稳定拿到 22 张
- 继续尝试能否压到 21 张，同时保持靠边与一刀通切
